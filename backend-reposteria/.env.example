APP_NAME=EncantoReposteria
APP_ENV=local
APP_KEY=base64:a+aaL+Mu5zCgIrpOUP1Sk0VSRIZOU5ULYizGy5tWu90=
APP_DEBUG=true
APP_URL=http://localhost:8000

# CONFIGURACIÓN POSTGRESQL
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=encanto_db
DB_USERNAME=postgres
DB_PASSWORD=julian

# ✅ CONFIGURACIÓN ESTABLE PARA SPA + SANCTUM
SESSION_DRIVER=file           # ← ARCHIVOS (no necesita tabla sessions)
SESSION_LIFETIME=120
CACHE_STORE=file              # ← ARCHIVOS (no necesita tabla cache)
QUEUE_CONNECTION=sync         # ← INMEDIATO (no necesita tabla jobs)

# Sanctum SPA
SANCTUM_STATEFUL_DOMAINS=localhost:4200,127.0.0.1:4200
FRONTEND_URL=http://localhost:4200